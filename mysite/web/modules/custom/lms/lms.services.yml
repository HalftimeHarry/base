services:
  lms.teamlist:
    class: Drupal\lms\TeamListService
    arguments: []

  logger.channel.lms:
    parent: logger.channel_base
    arguments: ['lms']

  lms.pool:
    class: Drupal\lms\PoolService
    arguments: ['@current_user', '@entity_type.manager', '@entity.query']
